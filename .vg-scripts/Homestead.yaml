---
ip: "192.168.10.10"
memory: 2048
cpus: 1

authorize: ~/.ssh/id_rsa.pub

keys:
    - ~/.ssh/id_rsa

sites:
    - map: laravel.loc
      to: /vagrant

variables:
    - key: APP_ENV
      value: localhost
